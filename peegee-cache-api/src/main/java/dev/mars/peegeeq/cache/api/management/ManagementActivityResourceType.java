package dev.mars.peegeeq.cache.api.management;

/** Bounded resource vocabulary exposed by process-local management activity. */
public enum ManagementActivityResourceType {
    SETUP,
    CACHE_ENTRY,
    COUNTER,
    LOCK,
    SUBSCRIPTION,
    PUBSUB_MESSAGE
}
